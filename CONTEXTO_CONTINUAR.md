# REYGAS — CONTEXTO PARA CONTINUAR (ACTUALIZADO)

> Exportado: 22/08/2026 - sesion de fixes: reporte redirect, ticket por pagar, auditoria/limpieza DB, destinos, edicion de pagos mixtos, Abonar Saldo, correlativos, credito (BUD-647), nube = fuente de verdad (H2W-236).
> Ultimo commit en main (origin): 1499867 - produccion: https://taller-two-gamma.vercel.app
> BUILD: el auto-updater (BuildAutoRefresh, /api/build-info) refresca solo; si el usuario ve build viejo: Ctrl+F5 una vez.
> **MODO AHORRO ACTIVADO**: respuestas compactas, tsc 1x por lote, 1 commit+push+deploy por lote, lecturas quirúrgicas (grep + solo zona a editar), sin verificación redundante.

---

## 0. MODO AHORRO (IMPORTANTE — activar en el nuevo chat)

El usuario pidió optimizar consumo de crédito de la API. Reglas para TODA la sesión:

1. **Agrupar cambios**: procesar los pedidos en UN lote → 1 tsc → 1 commit → 1 push → 1 verificación de deploy.
2. **Respuestas compactas**: confirmar en 2-4 líneas (qué se hizo → tsc ✓ → commit → deploy).
3. **Lecturas mínimas**: grep para localizar, leer SOLO la zona a editar (nunca archivos completos de 5000+ líneas).
4. **No verificar redundante**: si el usuario dice que se ve bien, seguir sin re-verificar.
5. **Checkpoint temprano**: si la sesión crece, actualizar CONTEXTO_CONTINUAR.md y abrir chat nuevo.

---

## 1. QUÉ ES EL PROYECTO

**ReyGas (repo GitHub nande100121-eng/taller)** — Sistema web de gestión integral de un taller de gas vehicular (GNV/GLP) en Perú.

- **Stack**: Next.js 14.2.4 (App Router), React 18, TypeScript 5.5, TailwindCSS 3.4, Zustand 4.5, Supabase (cloud-first + realtime), date-fns, lucide-react
- **Hardware**: Tablet industrial Chainway P80 + lector SEISA
- **Despliegue**: Vercel (git push auto-deploy). Prod: taller-two-gamma.vercel.app (Chrome Safe Browsing dio falso positivo; sin resolver).

### REGLAS OBLIGATORIAS DEL USUARIO (nunca violar)
- **Publicar cada cambio**: npx tsc --noEmit → commit en main → git push origin main (Vercel auto-deploy).
- **NO usar npx vercel --prod** (doble deploy gasta límite). Solo git push.
- **NUNCA modificar ni stagear CSVs** (registro taller *.csv, DEUDA 17.08.26.csv) ni scripts/_*.ps1, scripts/*.mjs, iniciar-dsh-web.bat (working tree tiene muchos sucios).
- Responder en español.

### Skills estándar (OBLIGATORIO según la tarea)
- reygas-supabase-congruence: SIEMPRE que se cree/modifique una acción que guarde/borre/sincronice datos en Supabase.
- reygas-ui-design-system: SIEMPRE que se agregue/modifique UI.
- reygas-reports-system: SIEMPRE que se cree/modifique un reporte.
- reygas-printing: SIEMPRE que se agregue/modifique impresión.
- reygas-performance-cloud-hardware: rendimiento, sync Cloud-First, tablets.

---

## 2. ARQUITECTURA Y ARCHIVOS CLAVE

### Módulos (src/app/dashboard/)
admin (CMS + Tabla Maestra), almacen, asistencia, caja, certificaciones, configuracion, consultas, porteria, recepcion, reportes, taller.

### Archivos núcleo
- src/lib/store/app-store.ts — Store Zustand persist (reygas-store-cache-v2). set envuelto en setWithLog (invoca updater UNA vez; CRITICAL — no duplicar). Acciones: registerInvoicePayment (crea inv-<Date.now()>), confirmInvoicePayment, updatePaymentRecord (dedup historial por id + conserva payment_breakdown real), deletePaymentRecord (si remaining 0 borra factura cascada), syncOperationalOnly/syncFromSupabase (dropean facturas locales de OTs tombstoned), getAndIncrementReceiptNumber (sanity cap folio < 999999).
- src/lib/supabase/services.ts — saveSupabaseWorkOrder (per-OT queue woSaveQueues, tombstone wo_deleted_ check, merge defensivo), saveSupabaseInvoice (filtro fantasma inv-<woId>, ANTI-GHOST tombstone wo_deleted_<id> → skip, __respectManualReceipt respeta N° digitado, resolveUniqueReceiptNumber cap < 999999, snapshots inv_full_/inv_payhistory_/inv_resources_/inv_breakdown_ por id Y woId, snapshots vacíos se borran), deleteSupabaseWorkOrder (CASCADA + tombstone), deleteSupabaseInvoice, fetchCappedOperationalData (ventana PAGE=400 + reconstrucción historial desde snapshots con cache 15s cappedHistoryCache + invalidar al guardar), fetchSupabaseDayReport.
- src/lib/system-log.ts — LOG LOCAL en localStorage (reygas-syslog-local, FIFO 12000): logSystemEvent, logTiming, initGlobalLogging (clicks/change/red/errores; interceptor filtra writes/errores/GET lentos >1500ms). NO usa Supabase.
- src/components/caja/thermal-receipt-modal.tsx — impresión térmica 80mm (boleta/ticket/factura + modo ticketPorPagar: TICKET POR PAGAR, sin correlativo, sin forma de pago, placa negrita, sin impuestos, DESCUENTO antes del TOTAL solo si >0).
- src/components/DailyWorkshopReportModal.tsx — reporte diario (REPORTE DEL DÍA, VENTAS POR CONCEPTO, YAPES & TRANSFERENCIAS POR DESTINO). Abonos respetan redirect_category; breakdownFromSources usa destination de cada split; dayPaymentsDeduped evita dobles.
NaN
- src/lib/utils/receipt-utils.ts — parseCorrelative (folio tras último guion), matchesReceiptSeries, formatReceiptNumber.

---

## 3. MODELO DE DATOS / CONCEPTOS CLAVE

### Zona horaria (CRÍTICO)
Perú = UTC−5. Fechas: getPeruDateString, buildPeruISOString (YYYY-MM-DDTHH:mm:00-05:00), toPeruDateKey. El reporte filtra por fecha de PAGO (no emisión).

### Vínculo recurso → pago (desde 17/08/2026)
- Cada pago guarda resources: [{description, category (servicio/repuesto/certificado), amount, receipt_number, receipt_type, redirect_category}].
- redirect_category (redirección del cajero): un recurso servicio puede redirigirse a repuesto/certificado — VENTAS POR CONCEPTO lo respeta (redirect_category || category).

### Pagos mixtos (1 comprobante, varios métodos)
- payment_breakdown guarda UN split por método: {method, destination, amount, splitResources, receipt_number, receipt_type}.
- DESTINO del comprobante: si todos los métodos coinciden → ese; si difieren → el del método de MAYOR monto. NUNCA se concatenan (CAJA / FRANCO fue el bug).
- Cada método tiene SU select de destino (NO hay select global — el usuario lo rechazó).
- Al EDITAR un mixto: handleOpenEditPaymentRecord reconstruye los splits del breakdown real (renormaliza keys de recursos contra el pool edit-res-*); el saldo restante de cada recurso es SECUENCIAL por método (método 1 toma 10 → saldo restante 120; método 2 dispone del faltante 120).
- Al EDITAR NO se valida contra el saldo (supera saldo solo en abonos nuevos); el monto REEMPLAZA, no suma.

### Facturas y snapshots (site_content)
- La tabla invoices NO tiene payment_history/breakdown/resource_payments como columnas — viven en snapshots: inv_full_<id> (prioridad), inv_payhistory_<id>, inv_resources_<id>, inv_breakdown_<id> — CADA UNO también por <work_order_id>.
- inv_full_<itemId> con category inventory = items del almacén (ids inv-RYG-*) — NO son facturas, NO tocar.
- wo_mod_<id> = snapshot de OT (descuento, assigned_technician_ids, items); wo_deleted_<id> = TOMBSTONE (bloquea re-creación); wo_removed_<id> = ítems eliminados.
- upsert site_content: POST/PATCH con on_conflict=section_key + Prefer resolution=merge-duplicates, body UTF-8.

### Descuento (NO es columna de work_orders)
Vive en wo_mod_<id>; el sync lo reconstruye. Al abonar, el cajero asigna el descuento a UN recurso (moveAbonoDiscount).

---

## 4. CAMBIOS RECIENTES (21/08 — sesión actual, en orden)

1. 18cc71b fix(reporte): abonos del día respetan redirect_category (recurso redirigido a Almacén/Certificados ya se distribuye en VENTAS POR CONCEPTO; caso AZX-546 TK01-00004607).
2. 2773fdc feat(taller): botón Ticket por Pagar en la card → modal térmico modo ticketPorPagar (sin correlativo, sin forma de pago, placa negrita, detalle de la card, solo TOTAL, sin op. gravadas/IGV).
3. ad25ce4 feat(ticket por pagar): DESCUENTO antes del TOTAL (solo si la card tiene descuento).
4. 9598baf fix(anti-fantasma/correlativo): (a) syncs dropean facturas locales de OTs borradas (tombstone); (b) sanity cap correlativo < 1M en getAndIncrementReceiptNumber.
5. 17cb822 fix(caja): al EDITAR se reconstruye el PAGO MIXTO real desde su desglose; DESTINO ÚNICO (select global — luego revertido).
6. 0273057 fix(caja): quitar el select de Destino global duplicado (cada método tiene el suyo); el destino se deriva de los métodos (mayor monto si difieren).
7. 46a8e52 fix(distribución mixta): rebuildBreakdownFromHistory DESANIDA Mixto (...) en métodos; rebuildDestFromHistory devuelve UN solo destino; la edición conserva el desglose real.
8. d631a47 fix(caja): al EDITAR ya NO se bloquea por supera saldo (el monto reemplaza; el saldo se recalcula); en edición se sincroniza el monto de cada método con sus recursos; dedup del historial por id.
9. 25628cc fix(caja edición): saldo restante SECUENCIAL por método (D7U-622 DIAFRAGMA S/130: método 1 toma 10 → saldo 120; método 2 dispone del faltante 120) — arregla keys desnormalizadas (abono-res-* vs edit-res-*).
10. 687fa20 fix(caja): Abonar Saldo SIEMPRE abre abono NUEVO (sin heredar editingRecordId); pagos RECONSTRUIDOS de la card (id rp-<invoiceId>/bd-, cuando el store no trae payment_history) ya NO abren edición (el lápiz abre Abonar Saldo; eliminar deshabilitado); el submit solo usa modo edición si el id existe en el historial real.
11. 256b354 fix(caja BEF-098 "abonar jala factura borrada"): clearInvoicePayments ("Borrar todos") retornaba SIN hacer nada si el store no traía payment_history (pagos reconstruidos rp-/bd-, historyCount 0) → la factura de prueba seguía en el caché con su crédito/comprobante y el modal de abono abría con sus datos. Ahora "Borrar todos" ELIMINA la factura COMPLETA en cascada (tabla + snapshots inv_full_/inv_payhistory_/inv_breakdown_/inv_resources_ por id y woId) + OT a por_cobrar, funcione o no con historial. Botón "Borrar todos" solo se muestra si la card tiene factura; toast/tooltip actualizados. (Verificado en prod 22:54: payment.clear_all.invoice_removed → invoice.delete.ok → abono abre invId null.)
12. 0f18a14 fix(caja abono BBF-936 "vincula recurso y jala el TOTAL no el saldo"): al marcar un recurso del comprobante el payAmount se limitaba a min(fullAmount, saldoDisponible del recurso) y para facturas pre-17/08 (fallback con pendingAmount=fullAmount) tomaba el TOTAL del recurso aunque el saldo pendiente fuera menor (BBF-936: total 450, saldo 50). Ahora el límite es el SALDO GLOBAL del comprobante (totalDue - invoicePaidSoFar - lo ya marcado en otros recursos/métodos): si el recurso es menor al saldo se completa al 100%; si es mayor solo se toma lo restante. Aplica a: click del checkbox, botón "✓ Todos" (reparto secuencial), input manual (max/val) y disabled cuando el saldo global ya está cubierto. En EDICIÓN no se limita (el monto reemplaza).
13. 8c02222 feat(portería): (a) la card "Citas & Reservas Programadas" ahora va ARRIBA de "Registrar Ingreso de Vehículo" (antes: ingreso → citas → semáforo; ahora: citas → ingreso → semáforo); (b) aviso inteligente en el campo PLACA: si la placa digitada tiene una cita/reserva pendiente o confirmada para la fecha seleccionada del ingreso (entryDate/selectedDate), se muestra un mensaje ámbar debajo del campo (plateHasAppointmentOnDate, compara scheduled_date slice(0,10) === fecha, normaliza placa sin [^A-Z0-9]).
14. b8893e4 feat(portería): el aviso del campo PLACA ahora también muestra si la placa tiene un INGRESO EN TALLER vigente con su estado (plateActiveWorkOrder: OT más reciente no finalizada, excluye GASTO; label via plateOrderStatusLabel: Ingreso/Diagnóstico/Esperando Repuestos/En Servicio/Por Pagar/Por Pagar (abonos)/Pagado-Listo para Entregar + contexto: pendiente de cobro en Caja, autorizado a salir, o en proceso de atención).
15. 985db2d feat: (a) portería: el aviso de ingreso en Taller del campo PLACA ahora incluye la FECHA/HORA de ingreso (formatPeruDateTime(entry_time)): "ingreso en Taller del DD/MM/AAAA HH:MM: ESTADO"; (b) taller: la card COLAPSADA muestra también "📅 Registro: fecha/hora" junto a OT/ítems/cliente.
16. fix(portería duplicado A3Z-265) — commit de este lote: al REGISTRAR ingreso se BLOQUEA si la placa ya tiene OT ACTIVA (no finalizada, excluye GASTO): notify warning con estado + fecha + "No se registró un ingreso duplicado" y log porteria.ingreso_bloqueado_duplicado. El aviso inline del campo placa ahora es ROJO prominente "⚠️ YA REGISTRADO" con la fecha y estado (era cian sutil).
17. CORRELATIVOS INTELIGENTES (1a76ec9): nueva fetchLatestReceiptMaxima(7) en services.ts consulta Supabase (invoices) los últimos 7 días por serie y devuelve el folio máximo real de TK01/B001/F001/FC01. Nueva acción del store syncReceiptMaximaFromCloud() eleva correlativeConfig (ticket/boleta/factura/notaCreditoLastNumber) si la nube tiene folios mayores; se llama al final de syncFromSupabase, syncOperationalOnly y al abrir los modales de pago/abono en Caja (handleOpenPaymentModal/handleOpenPartialPaymentModal). Así los nuevos comprobantes continúan la secuencia real (último usado +1) y no reusan números ya emitidos en otra tablet. NOTA: ese primer intento traía 2 bugs corregidos en el punto 18.
18. CORRELATIVOS — CORRECCIÓN (21/08, commit de este lote): (a) fetchLatestReceiptMaxima() en services.ts consultaba created_at (columna INEXISTENTE en invoices) → la query fallaba y el sync moría en silencio; ahora selecciona SOLO receipt_number/issued_at/paid_at y descarta filas con fecha ilegible. (b) Regla del usuario: la secuencia continúa desde los ÚLTIMOS correlativos reales (día anterior + día actual, Perú). Se eliminó la dependencia del folio máximo del CSV histórico que inflaba el preview del abono: ticket 6803 (fila 10/01/2026 sin tipo) → daba 6804; boleta 4218 (24/07) → 4219; factura 2658 (07/05) → 2659. La nube es la fuente de verdad: como los folios son secuenciales, el máximo real = último emitido (TK01→4620, B001→276, F001→290, emitidos 18/08) y el próximo comprobante es TK01-4621 / B001-277 / F001-291; nunca se reusa un folio ya emitido. (c) getAndIncrementReceiptNumber (store) y workshopMaxCorrelative (caja) ahora solo cuentan registros del CSV y facturas locales con fecha de AYER u HOY (dateISO / toPeruDateKey(issued_at||paid_at)), con tope de cordura < 1M.
19. AUTO-ACTUALIZACIÓN DE BUILD (22/08, commit de este lote): nueva ruta /api/build-info devuelve el SHA del build DESPLEGADO (VERCEL_GIT_COMMIT_SHA, force-dynamic, sin cache). Nuevo componente BuildAutoRefresh (montado en src/app/dashboard/layout.tsx, junto a Toast) compara cada 3 min y al recuperar el foco de la pestaña el build local (BUILD_SHA de system-log) contra /api/build-info: si hay versión nueva, log app.build.stale + banner ámbar "🆕 Nueva versión (XXXX) disponible" y recarga SOLO tras 6 s (botón "Recargar ahora"). MOTIVO: el log del 22/08 03:05-03:09 mostró 4 builds activos a la vez (034fe62f, 1a76ec9, 5f3a9eac, baedd570) pisándose datos en la MISMA OT (CHE-624 81f06faf: total 520→650→670→630, ítem KIT desaparecía y reaparecía, estado esperando_repuestos→por_cobrar) y un POST inventory_items 400 desde el build viejo (schema de payload distinto; el merge defensivo de saveSupabaseWorkOrder ya protege dispatched/updated_at, pero un build VIEJO no tiene esos parches). El auto-updater hace que TODOS los dispositivos corran el mismo build y los viejos dejen de escribir. ACCIÓN USUARIO: hacer Ctrl+F5 en TODOS los dispositivos UNA vez (el auto-updater solo existe en el build nuevo; tras eso se auto-actualizan solos).
20. FIX CAMBIO DE COMBUSTIBLE EN TALLER (22/08, commit de este lote): placa H2W-236 no dejaba cambiar fuel_type (fecha 19.08). Causa raíz: fetchCappedOperationalData capa vehicles a 400 por last_visit_date DESC; H2W-236 tiene last_visit_date 2025-06-12 (2.114 vehículos más recientes en la tabla de 3.323) → NO estaba en state.vehicles → la card mostraba el select con el fallback "GNV" y updateVehicle no encontraba la placa en el array → el save NUNCA se disparaba (estaba dentro del map de match) → el select volvía a GNV tras cada intento (log: vehicle.update repetido GLP/Bifuel/GLP sin persistir). Fix: updateVehicle ahora detecta placa FUERA de la ventana → lee el vehículo real de la nube (nuevo fetchSupabaseVehicleByPlate en services.ts, eq+ilike), aplica el cambio, guarda (saveSupabaseVehicle) y lo INYECTA al store local; el merge por placa de syncOperationalOnly (Map que conserva locales) lo mantiene entre syncs. Log vehicle.update incluye inWindow; nuevo log vehicle.update.out_of_window.
21. CAJA — MONTO 0 => SIN COMPROBANTE + LIBERACIÓN DE CORRELATIVO (22/08, commit de este lote): (a) el modal de ABONO PARCIAL (handleOpenPartialPaymentModal) ahora abre con "Sin Comprobante" cuando totalDue <= 0 (antes default Ticket → consumía correlativos en cards de S/0); el modal de cobro completo ya lo hacía (line 1413). (b) Red de seguridad en los 3 submits (paymentModal/partial/manual): si el monto es 0 se trata SIEMPRE como "Sin Comprobante" (isSinComprobante/isSinCompAbono con || isZeroAmount/amount<=0) → no se asigna ni avanza correlativo. (c) LIBERACIÓN al editar: en el submit de edición (partialPaymentModal con editingRecordId) cuando es "Sin Comprobante" se pasa receipt_number/receipt_type en "" (antes pasaba undefined → el store conservaba el N° viejo con __respectManualReceipt → log invoice.correlativo.manual_respetado con TK01-4622/623/624/625 de cards S/0 sin liberar). (d) updatePaymentRecord (store) limpia el N° en el historial (merge explícito receipt_number "" ) → la factura guarda receipt_number null; nueva acción recomputeReceiptMaximaAfterClear() recalcula correlativeConfig al último folio REAL (nube + facturas locales) y SÍ puede BAJAR (liberación: el N° vuelve a estar disponible; el sync normal solo eleva). Se dispara tras editar a Sin Comprobante (log payment.record_update.receipt_liberado + correlative.release.after_clear) y al Borrar Todos (clearInvoicePayments, log payment.clear_all.receipt_liberado).
22. LIBERACION CORRELATIVO - 2do FIX (22/08, commit de este lote): el log del 22/08 03:44-03:49 (build a448d15b) mostro que editar a "Sin Comprobante" SEGUIA guardando TK01-4622/623/624/625 (invoice.correlativo.manual_respetado). Causa: en updatePaymentRecord el N. de la factura era `lastRec?.receipt_number || targetInvoice.receipt_number || ""` -> al limpiar, lastRec.receipt_number era "" (falsy) y CAIA AL N. VIEJO de la factura (targetInvoice.receipt_number = TK01-4622). Fix: si la edicion paso explicitamente receipt_number/receipt_type en "" (Sin Comprobante / monto 0) la factura guarda "" SIN fallback; si no (edicion normal) conserva el comportamiento anterior. Ademas recomputeReceiptMaximaAfterClear ahora hace UNA re-verificacion a los 8 s (el save es async y la consulta a la nube podia leer ANTES de que el POST aterrizara, dejando config en 4625 aunque el N. ya estuviera liberado). Con esto: editar las 4 cards S/0 (A3Z-265, CYL-161, A6N-269, A2D-119) las limpia y la secuencia vuelve a TK01-4621.
23. LIBERACION - 3er FIX (22/08, commit de este lote): el log del 22/08 03:58-03:59 (build 6004d4f9) mostro que la factura SI se limpiaba (invoice.save.ok receipt "" en A3Z-265/CYL-161/A6N-269/A2D-119) pero NO aparecian los logs payment.record_update.receipt_liberado/correlative.release.after_clear -> la config quedaba en 4625. Causa: clearedReceipt solo revisaba current.receipt_number (el registro del historial reconstruido desde el snapshot inv_payhistory_ viene SIN el N.); el N. vive en la factura (targetInvoice.receipt_number = TK01-4622). Fix A: clearedReceipt usa current.receipt_number || targetInvoice.receipt_number. Fix B (auto-correccion): syncReceiptMaximaFromCloud ahora detecta config > maximo real de la nube y dispara recomputeReceiptMaximaAfterClear (que baja al max(local, nube)), asi la config se auto-corrige en cada sync aunque las facturas ya esten limpias (no requiere re-editar).
24. PORTERIA - PERMITIR NUEVO INGRESO SI LA OT ESTA PAGADA/LISTA (22/08, commit de este lote): una placa con OT en 'pagado_autorizado' (Pagado / Listo para Entregar - autorizado a salir, Semafaro Verde) mostraba 'YA REGISTRADO' y BLOQUEABA el ingreso (el bloqueo solo excluia finalizado/GASTO). Fix en porteria/page.tsx: (a) handleRegisterEntry ya NO bloquea si la OT de la placa esta pagado_autorizado (solo bloquea en proceso: ingresado/en_diagnostico/esperando_repuestos/en_servicio/por_cobrar/pendiente_pago; la placa con ingreso del 07/08 podra registrarse de nuevo); (b) el aviso del campo placa se divide: si la OT esta pagado_autorizado muestra una nota VERDE informativa 'Ingreso anterior del DD/MM/AAAA HH:MM: PAGADO - autorizado a salir (Semafaro Verde). Puede registrar un nuevo ingreso.' (CheckCircle2) y NO el rojo 'YA REGISTRADO... no se registrara otro ingreso duplicado' (ese queda solo para OTs en proceso).
25. PORTERIA - 'POR PAGAR' TAMPOCO BLOQUEA (22/08, commit de este lote): placa CKD-567 con OT en por_cobrar/pendiente_pago (label 'Por Pagar') del 06/08 seguira bloqueando el ingreso con el toast 'YA tiene un ingreso en Taller (Por Pagar)'. El usuario indica que 'por pagar puede ser un credito' y debe dejar registrar OT. Fix en porteria/page.tsx: nuevo PORTERIA_BLOCK_STATUSES = Set([ingresado, en_diagnostico, esperando_repuestos, en_servicio]) — SOLO esos estados bloquean el ingreso duplicado; por_cobrar, pendiente_pago, pagado_autorizado y finalizado NO. handleRegisterEntry usa el Set. El aviso del campo placa ahora tiene 3 ramas: ROJO bloquea (en proceso), VERDE informativo (pagado_autorizado) y AMBAR informativo (por_cobrar/pendiente_pago: 'pendiente de cobro en Caja (puede ser credito). Puede registrar un nuevo ingreso.').
26. CAJA - EMITIR CORRELATIVO CON MONTO 0 (credito/pendiente) (22/08, commit de este lote): BUD-647 (19.08) necesita emitir el ticket TK01-4639 con monto 0 porque queda como pendiente/credito, pero el sistema forzaba 'Sin Comprobante' con monto 0 y bloqueaba (reason sin_monto). Fix en caja/page.tsx: el DEFAULT al abrir sigue siendo 'Sin Comprobante' con monto 0 (regla 21/08), pero si el cajero elige EXPLICITAMENTE Ticket/Boleta/Factura se respeta: se quita el '|| isZeroAmount'/'|| amount <= 0' de isSinComprobante/isSinCompAbono en los 3 submits (paymentModal/partial/manual), el bloqueo 'amount <= 0 && !isEditingRecord' ahora solo aplica si es Sin Comprobante (isSinCompAbono), y el recurso obligatorio solo se exige si amount > 0. Asi el cajero puede elegir Ticket, escribir/aceptar el N. (TK01-00004639) y emitir el comprobante de credito con monto 0.
27. CAJA - MONTO DEL ABONO SIEMPRE DEL DESGLOSE (fix 22/08, commit de este lote): al emitir el ticket de credito con monto 0 (BUD-647) salia 'La suma de los abonos parciales (S/ 0.00) debe coincidir con el monto a abonar (S/ 150.00)'. Causa: el calculo del monto caia al valor inicial del modal (partialPaymentModal.amount = balance 150) cuando los splits estaban en 0 (el cajero puso 0 para no cobrar) -> amount 150 vs splits 0 => bloqueo suma_splits. Fix: el monto del abono SIEMPRE es la suma de los splits (o de los recursos marcados), SIN caer al modal.amount inicial; asi con splits 0 y Ticket explicito el abono queda en S/ 0.00, se emite el correlativo (TK01-4639) y la OT conserva el credito (isFullyPaid false -> pendiente).
28. TICKET DE CREDITO - FIX DEFINITIVO + BOTON CREDITO (22/08, commit de este lote): el log 05:14-05:19 mostro que el bloqueo 'suma_splits amount 150 totalSplits 0' seguia en c588571b. Causa real: los RECURSOS vienen PRESELECCIONADOS con su saldo (150) y selectedSum (150) tomaba precedencia sobre los splits (0) => amount 150 aunque el metodo estuviera en 0. Fixes en caja/page.tsx: (1) amount = Number(totalSplitsSum.toFixed(2)) SIEMPRE (la suma de los METODOS es el pago; los recursos preseleccionados NO dominan); (2) con amount <= 0 NO se vinculan recursos (abonoResources solo si amount > 0) => no bloquea 'marque recurso' ni 'suma_recursos'; (3) nuevo boton '💳 Credito - emitir comprobante sin cobrar (S/ 0.00)' en la seccion 2 del modal de abono: un clic pone TODOS los metodos en 0 y desmarca recursos (handleCreditTicketMode, log abono.mode_credito); luego el cajero elige Ticket, escribe TK01-00004639 y Confirma => correlativo emitido y total pendiente (credito).
29. NUBE = FUENTE DE VERDAD - DESCARTAR FACTURAS LOCALES FANTASMA (22/08, commit de este lote): la card de H2W-236 mostraba un historial de pago con fecha 19.08 que NO aparecia en el reporte diario. Verificado en Supabase: NO existe factura ni snapshot (inv_payhistory_/inv_full_/inv_breakdown_/inv_resources_) para la OT 86723e54 (entry 18/08, total S/280, status pagado_autorizado en la nube). El pago vivia SOLO en el cache local: syncOperationalOnly/syncFromSupabase conservaban las facturas locales que la nube ya no tiene (solo dropeaban si la OT tenia tombstone wo_deleted_), asi la card mostraba pagos que el reporte (construido 100% desde la nube via fetchSupabaseDayReport) no podia incluir. Fix en app-store.ts (ambos merges de facturas): la LISTA REMOTA es autoritativa; las facturas locales sin equivalente en la nube se DROPEAN del store (log invoice.local_only_descartada con count/ejemplos). Excepcion de guardado en vuelo: si hay mutacion local de facturas < 8s (hasRecentLocalMutation('invoices', 8000), el upsert async aun no aterrizo) se conservan hasta el proximo sync. NO toca el merge de vehiculos (el fix de H2W-236 fuel_type necesita conservar vehiculos fuera de la ventana de 400). USUARIO: en la card H2W-236 borrar el abono fantasma (Borrar todos) y volver a generar el pago con fecha 19.08 para que persista en la nube y aparezca en el reporte del 19.08.
30. PORTERIA - BOTON 'NO ASISTIO' + HISTORIAL DE CITAS (22/08, commit de este lote): en las cards de Citas & Reservas de Porteria se agrego el boton 'No asistio' (icono XCircle, gris) junto a Confirmar Asistencia / Reprogramar / Anular. Al pulsarlo pide confirmacion y guarda el estado 'no_asistio' en Supabase (updateAppointmentStatus -> saveSupabaseAppointment, snapshot appt_<id>), log porteria.cita.no_asistio. El estado 'no_asistio' se agrego al union del interface Appointment (app-store.ts) y se excluye de la lista activa de citas pendientes. Nueva seccion HISTORIAL al final de la card de citas (icono History): muestra las citas del dia con estado no_asistio / anulada / completada (placa, cliente, hora, badge) - max 12, scroll, guardadas en la nube. El badge de la card activa ahora es dinamico (CONFIRMADO verde / PENDIENTE ambar; antes siempre decia CONFIRMADO). Recepcion actualizado para el nuevo estado: chip 'No Asistieron' + filtro quickFilter 'no_asistieron', badge de card gris 'No Asistio', opcion en el select de edicion, conteo no_asistidas en el Informe Diario (tabla + imprimible + linea de totales).
31. LOGIN POR USUARIO (ROSTER) (22/08, commit de este lote): el login web exigia '@' (input type=email) y el store login() no validaba usuario ni contrasena (cualquier texto entraba como 'personal' con rol Operador de Taller). Fix: (a) en src/app/login/page.tsx el input pasa a type=text (placeholder 'usuario o correo', label 'Usuario / Correo Electronico'); (b) el store login() (app-store.ts) busca en technicians por username (case-insensitive), email, full_name o usuario generado (generateDefaultUsername) y VALIDA la contrasena del roster (t.password; si esta vacia usa el default = usuario generado, igual que addTechnician); si no hay match o la contrasena no coincide devuelve false -> la pagina muestra 'Credenciales no validas'; (c) se conservan los accesos demo admin@reygas.com / personal@reygas.com (botones 1-clic) y el acceso admin por nombre que contenga 'admin'; (d) currentUser agrega campo username opcional. Los usuarios reales del roster (jreyes, kcalixto, jquinteros, crodriguez, jsandoval, isabel, jpadilla, dagurto, crosas, jpolo, byguia, amendoza, gcaceres, rrojasramirez) ya inician sesion con su usuario + contrasena del roster (Tabla Maestra -> Roster y Permisos / cambiar-clave).
32. LOGIN VINCULADO A SUPABASE AUTH (@reygas.com) + PESTAÑAS ESTRICTAS (22/08, commit de este lote): (a) nuevo src/lib/supabase/auth.ts: cliente dedicado supabaseAuth (storage propio 'reygas-auth-session'; el cliente de datos sigue 100% anon -> NO rompe REST/RLS), composeAuthEmail (usuario -> usuario@reygas.com), provisionSupabaseAuthUser (idempotente: si ya existe con misma password -> linked; si no -> signUp; si existe con otra -> password_mismatch), verifySupabaseAuthLogin, signOutSupabaseAuth. (b) store (app-store.ts): addTechnician / updateTechnician (si cambiaron usuario o password) / changeTechnicianPassword llaman provisionSupabaseAuthUser (email = usuario@reygas.com + password del roster); login() ahora es ASYNC: primero verifica en Supabase Auth (signInWithPassword con usuario@reygas.com), si ok resuelve el perfil del roster (username / local-part del email / email / full_name / usuario generado) para allowed_tabs; fallback al roster si el auth user aun no esta vinculado o sin confirmar; logout cierra tambien la sesion de auth. (c) login page: handlers async (await login) + placeholder 'usuario@reygas.com o usuario'. (d) layout.tsx: guard ESTRICTO - se elimina el bypass de allowed_tabs vacio (ahora usa ALL_ERP_STATIONS_DEFAULT como set por defecto, NUNCA 'todas'); el sidebar filtra por pestañas autorizadas y el acceso por ENLACE DIRECTO a una ruta no autorizada muestra la pantalla 403 'Acceso No Autorizado' con boton 'Volver a mi Estacion Permitida' (ya existia). OJO ADMIN: el proyecto Supabase tiene 'Confirm email' ON (mailer_autoconfirm:false) -> los usuarios creados con signUp quedan sin confirmar y el login cae al fallback roster hasta desactivar 'Confirm email' en Supabase Dashboard (Authentication -> Sign In/Up -> Email). Passwords < 6 chars no se pueden crear en auth.users (policy) -> esos quedan en fallback roster.
33. DEPLOY STALE - VERCEL NO DESPLEGO 76024df NI 5568819 (22/08, commit de este lote): /api/build-info de produccion mostro SHA af60936b (commit del boton No Asistio) -> el usuario seguia en el build viejo y al entrar con un usuario del roster aparecia 'Operador de Taller' con TODAS las pestañas (login viejo: full_name match fallaba -> nombre generico, sin allowed_tabs; layout viejo: allowed_tabs vacio = todo). Accion: (a) se limito el acceso demo 'personal@reygas.com' a SOLO /dashboard/porteria (nombre 'Operador de Taller (demo)') para que el boton 1-clic nunca otorgue todas las pestañas; (b) este commit re-dispara el deploy de TODO main (5568819 incluido); si /api/build-info sigue viejo, hacer git commit --allow-empty -m 'chore: re-trigger vercel deploy' + push. El login correcto (76024df+5568819) ya valida username+password del roster, vincula con Supabase Auth (@reygas.com) y respeta allowed_tabs (403 si se entra por enlace).
34. SESION PERSISTENTE + GUARD DE AUTENTICACION (22/08, commit de este lote): el usuario entro con su usuario del roster pero al cambiar la URL a /dashboard/caja (recarga completa) 'entro como administrador'. Causa raiz: (a) partialize del store NO persistia la sesion (isAuthenticated/userRole/currentUser fuera del cache) -> al recargar, el store se hidrataba SIN sesion; currentUser null -> el badge del layout mostraba el fallback 'Administrador ReyGas' y las pestanas caian al set por defecto (todas) = acceso 'como admin'; (b) el layout NO tenia guard de autenticacion (sin isAuthenticated no redirigia a /login). Fix: (1) partialize ahora persiste isAuthenticated, userRole, currentUser e isVisualEditing -> el login sobrevive recargas y cambios de URL; (2) layout.tsx: nuevo guard - si !isAuthenticated hace router.replace('/login') (el dashboard jamas se renderiza sin sesion); (3) badge de sesion con fallback honesto 'Sesión sin perfil' (nunca 'Administrador ReyGas' por defecto). Nota: los cachés viejos (guardados sin sesion) pediran login una vez tras el deploy.
35. LOGIN SIN ACCESOS RAPIDOS + CREDENCIALES DE ADMIN (22/08, commit de este lote): se eliminaron los botones 'Acceso Rápido de Prueba' (admin y personal taller) de la pagina de login y los paths demo del store (personal@reygas.com / 'Operador de Taller (demo)' ELIMINADO). El login de ADMINISTRADOR ahora requiere CONTRASEÑA: usuario 'admin' o 'admin@reygas.com' + ADMIN_PASSWORD (constante exportada en app-store.ts: 'ReyGas2026'). Se elimino el match suelto demo.includes('admin') (antes cualquier nombre que contuviera 'admin' daba acceso admin sin password). El login admin primero intenta Supabase Auth (admin@reygas.com) y si no esta provisionado usa la constante. Login page: imports limpios (Mail/KeyRound/ArrowRight), redirect exacto (solo 'admin'/'admin@reygas.com' -> /dashboard/admin/cms). DEPLOY PENDIENTE: Vercel alcanzo el limite diario (prod en ee27c7e); cuando se resetee (00:00 UTC ~7pm Peru) hacer Redeploy del ultimo commit en el dashboard o empty commit.
36. SESION PERSISTENTE REFORZADA - CARTERA DE SESION SINCRONA (22/08, commit de este lote): el usuario reporto que al recargar la web le pedia login de nuevo (aun estando desplegado a0c4969, que ya persiste la sesion en el cache). Causas posibles: (a) write del cache DIFERIDO 3s (debounce) -> recargar < 3s tras el login perdia la sesion; (b) cuota de localStorage llena -> setItem fallaba y NO se guardaba NADA (ni sesion). Fix en app-store.ts: (1) nueva CARTERA DE SESION 'reygas-session-wallet-v1': key diminuta con escritura SINCRONA en login() (admin + 3 rutas personal) y limpieza en logout(); getItem la fusiona SIEMPRE sobre el cache (la sesion de la cartera gana aunque el cache grande no se haya escrito). (2) setItem: debounce reducido de 3000 a 1000ms + FALLBACK POR CUOTA: si el JSON grande no entra, se persiste SOLO la sesion (isAuthenticated/userRole/currentUser/isVisualEditing) para que el login sobreviva. Resultado: el login persiste entre recargas y cambios de URL aunque el cache grande falle o la recarga sea inmediata.

### Estado de la base (21/08 — limpieza hecha)
- 118,082 facturas auditadas: 0 tickets absurdos, 0 correlativos duplicados con serie, 0 facturas fantasma en la tabla. (Los 7,926 duplicados restantes son folios históricos SIN serie de CSVs — NO tocar.)
- 84 snapshots huérfanos eliminados (15 inv_full_, 4 inv_payhistory_, 1 inv_breakdown_, 64 wo_mod_) + 64 tombstones wo_deleted_ creados. Verificado: 0 huérfanos restantes.
- Destinos dobles corregidos a FRANCO: TK01-00004617, TK01-00004585, TK01-00004608, TK01-00006804 (tabla + snapshots). 0 destinos con / en la base.
- BEF-098 restaurada: factura bef098-restaurada-0001 (TK01-00004610, total S/2800, abono Transferencia S/1000 el 18/08, saldo S/1800, destino FRANCO) vinculada a OT 268bffbe — con snapshots por id y woId. La factura original se perdió al borrar una OT (cascada).
- 21/08: los pagos mixtos de D7U-622 (180), BBL-219, T2V-669, BVZ-412 siguen en la base con sus desgloses (splits con destino FRANCO).

---

## 5. PENDIENTES / PRÓXIMOS PASOS

- **VERIFICAR deploy de 687fa20 (y 25628cc) en prod**: Vercel no desplegó automático. Ctrl+F5; si sigue viejo → deploy manual del usuario o empty commit re-trigger.
- El usuario sigue probando Caja (editar comprobantes, abonos, pagos mixtos, reporte 18/08) — corregir inconsistencias (modo ahorro).
- Eliminar la factura de prueba de BEF-098: con 256b354, pulsar "Borrar todos" en la card ya elimina la factura completa aunque el historial sea reconstruido. (Alternativa manual: borrar el pago REAL pay-bef098-1000-1808 — cascada.)
- Pago mixto con destinos distintos (Efectivo→CAJA + Yape→FRANCO en un solo comprobante): YA se soporta — cada método guarda monto+destino; la matriz YAPES muestra el yape en su destino; el destino del comprobante es único (mayor monto).
- Mejora pendiente (mencionada antes): Almacén reducir carga inicial de inventario completo.

---

## 6. ERRORES COMUNES / TRAMPAS AL EDITAR

- Al editar archivos con template literals anidados (backticks dentro del código run_code): construir por concatenación de líneas (array join) o comillas simples externas; un backtick interno rompe con Expected ','. Los ${...} en old_string también se interpolan → usar comillas simples.
- PowerShell 5.1: no existe || (usar if); pwsh no existe (usar powershell o & .\scripts\x.ps1); PATCH/POST body debe ser bytes UTF-8; ConvertFrom-Json no admite añadir props (reconstruir con [ordered]); Invoke-WebRequest falla en modo no interactivo (curl.exe o Invoke-RestMethod).
- El log es LOCAL (localStorage): no consultar Supabase; pedir Configuración → Log Interno → Ver Log/Descargar JSON/Copiar.
- El campo build del log = NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA.slice(0,8) — sirve para verificar qué commit está desplegado (buscar el SHA en los chunks JS de producción).
- saveSupabaseInvoice omite fantasmas (inv-<woId>), salta si la OT tiene tombstone, respeta N° manual (__respectManualReceipt).
- setWithLog invoca el updater UNA vez — nunca duplicar (causaba dobles facturas/abonos/instalaciones).
- No stagear CSVs ni scripts/_* ni iniciar-dsh-web.bat.
- Si push no dispara deploy: git commit --allow-empty -m "chore: re-trigger vercel deploy" + push (o deploy manual).
- El MiniDatePicker usa createPortal al body: verificar popupRef al cerrar.
- Los pagos reconstruidos (rp-/bd-) NO tienen registro real: no editar/eliminar directamente (usar Abonar Saldo).
